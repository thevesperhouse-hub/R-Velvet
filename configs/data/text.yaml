# Text dataset configuration
train_path: data/train.bin
val_path: data/val.bin
seq_len: 2048
stride: null              # null = seq_len (no overlap)
tokenizer: data/velvet_tok_100k_unigram
num_workers: 4
